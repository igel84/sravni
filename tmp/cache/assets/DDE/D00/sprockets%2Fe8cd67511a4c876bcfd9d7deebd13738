o: ActiveSupport::Cache::Entry	:@compressedF:@expires_in0:@created_atf1349174111.2393773:@value{	I"length:EFiI"digest;
F"%6547caeac615c47fd726e517c9f226e2I"source;
FI"
  jQuery.fn.my_upload = function() {
    return $('.new_xml_file').fileupload({
      dataType: "script",
      add: function(e, data) {
        var file, types;
        types = /(\.|\/)(xls)$/i;
        file = data.files[0];
        if (types.test(file.type) || types.test(file.name)) {
          data.context = $(tmpl("template-upload", file));
          $(this).append(data.context);
          return data.submit();
        } else {
          return alert("" + file.name + " is not a xls file");
        }
      },
      progress: function(e, data) {
        var progress;
        if (data.context) {
          progress = parseInt(data.loaded / data.total * 100, 10);
          return data.context.find('.bar').css('width', progress + '%');
        }
      }
    });
  };
;
FI"_version;
F"%3feaa12dca3915c66d20f3a896febcb5